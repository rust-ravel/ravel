// use dotenv::dotenv;
// use crate::;

fn main() {
    // TODO: this in the future.

    // crate::ravel::boot();

    // // Tell Cargo that if the given file changes, to rerun this build script.
    // // if config
    // match config!("DB_CONNECTION") {
    //     Some(db_connection) => match db_connection {
    //         "mysql" => {
    //             #[cfg(all(feature = "database-mysql"))]
    //             compile_error!("feature \"database-mysql\" must be enabled when using a \"mysql\" DB_CONNECTION.");
    //         },
    //         "postgres" => {
    //             #[cfg(all(feature = "database-postgres"))]
    //             compile_error!("feature \"database-postgres\" must be enabled when using a \"postgres\" DB_CONNECTION.");
    //         },
    //         "sqlite" => {
    //             #[cfg(all(feature = "database-sqlite"))]
    //             compile_error!("feature \"database-sqlite\" must be enabled when using a \"sqlite\" DB_CONNECTION.");
    //         },
    //         _ => {
    //             compile_error!("Invalid DB_CONNECTION. Supported values are <mysql|postgres|sqlite>.");
    //         }
    //     },
    //     None => {}
    // }
}